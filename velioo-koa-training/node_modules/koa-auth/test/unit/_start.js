var koaAuth = require('./../../');

describe('koa-auth', function() {
  it('Should exist', function() {
    koaAuth.should.beinstanceOf(Function);
    koaAuth.Provider.should.be.instanceOf(Function);
  });
});
